fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator>=2.0.0
boto3>=1.35.0
Pillow>=11.0.0
# Brochure ingestion: extracts embedded images and text from car PDFs
PyMuPDF>=1.24.0
# Diagnosis knowledge base: reads the editor-facing .xlsx workbook
openpyxl>=3.1.0
opensearch-py>=2.7.0
aiosqlite>=0.20.0
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
redis==5.2.1
slowapi==0.1.9
prometheus-client==0.21.1
# 3.4.0, not 3.3.0: PYSEC-2024-232/233 are an algorithm-confusion flaw and a
# decompression bomb in JWT handling, and core/security.py signs and verifies
# our tokens with this. PYSEC-2025-185 has no fix upstream and is accepted —
# it is tracked in docs/PRODUCTION_RELEASE_PLAN.md rather than silently.
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt<4.0.0
# 0.0.31 closes six advisories in multipart parsing, all of them shapes of
# resource exhaustion on a request the server has already accepted — which is
# the file-upload path, reachable by anyone who can POST.
python-multipart==0.0.31
httpx==0.28.1
aiosmtplib>=3.0.0
langchain==0.3.14
langchain-community==0.3.14
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
razorpay>=1.3.0
apscheduler>=3.10.0
qdrant-client>=1.9.1
fastembed>=0.3.6
# WAVE 3 ML features
sentence-transformers>=2.2.0
ultralytics>=8.0.0
pytesseract>=0.3.10
pgvector>=0.1.0

# Roadside repair marketplace — UPI scan-to-pay QR rendering.
# Optional: services/upi.py degrades to a tappable UPI link without it.
qrcode[pil]>=7.4
